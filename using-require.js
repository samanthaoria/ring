const toTitleCase = require("to-title-case");
const toTittleCase = require("to-title-case");
console.log(toTittleCase);

const lower = "The lord of the rings: the return of the king";
const tittle = toTitleCase(lower);

console.log(lower);
